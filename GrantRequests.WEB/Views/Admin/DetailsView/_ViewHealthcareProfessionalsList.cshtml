@model IEnumerable<GrantRequests.WEB.Models.HealthcareProfessionViewModel>

<p>
    @Html.ActionLink("Create New", "EditHealthcareProfessionals")
</p>
<table class="table">
    <tr>
        <th class="col-md-6">
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th class="col-md-6">Action</th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditHealthcareProfessionals", new { id=item.Id }) 
        </td>
    </tr>
}

</table>
